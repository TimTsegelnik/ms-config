feign:
  autoconfiguration:
    jackson:
      enabled: true

keycloak:
  realm: gate
  auth-server-url: http://host.docker.internal:8080/auth
  resource: gate
  ssl-required: external

swagger:
  project-title=@project: Microservices
  project-version: 1.0
  project-description: No pain no gain (KlSMEQKWI1ssCcp3mn5YgpDsbNgmcdWB)

springdoc:
  swagger-ui:
    csrf:
      enabled: true